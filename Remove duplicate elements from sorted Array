class Solution {
    int remove_duplicate(int a[],int N){
        
        int j=1;
        for(int i=0;i<N-1;i++)
        {
            if(a[i]!=a[i+1])
            {
                
                a[j]=a[i+1];
                j++;
            }
        }
        return j;
    }
}
